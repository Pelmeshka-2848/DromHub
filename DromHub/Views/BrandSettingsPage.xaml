<Page x:Class="DromHub.Views.BrandSettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:models="using:DromHub.Models"
      x:Name="PageRoot">

    <Page.Resources>
        <Style TargetType="Expander">
            <Setter Property="ExpandDirection" Value="Down"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Margin" Value="20,0,20,0"/>
        </Style>
    </Page.Resources>

    <!-- Контейнер для фиксированной кнопки -->
    <Grid>
        <!-- единый скролл на всю страницу -->
        <ScrollViewer VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <!-- Аккордеон: 4 строки -->
            <Grid x:Name="Root" RowSpacing="10" Margin="0,0,0,60">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- === ОСНОВНЫЕ === -->
                <Expander x:Name="ExpCore"
                          Grid.Row="0"
                          Expanding="Accordion_Expanding">
                    <Expander.Header>
                        <TextBlock Text="ОСНОВНЫЕ"
                                   Style="{ThemeResource SubtitleTextBlockStyle}"
                                   FontFamily="{StaticResource SFPro}"
                                   Margin="10,4,10,4"
                                   VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis"/>
                    </Expander.Header>

                    <Grid Margin="10,4,10,4" ColumnSpacing="12" RowSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="420"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Name -->
                        <FontIcon Grid.Row="0" Grid.Column="0" Glyph="&#xE70F;" VerticalAlignment="Center"/>
                        <StackPanel Grid.Row="0" Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Название бренда"
                                       FontFamily="{StaticResource SFPro}"
                                       Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                            <TextBlock Text="Влияет на отображение и поиск; при изменении выполняется нормализация и переиндексация."
                                       FontFamily="{StaticResource SFPro}"
                                       Style="{ThemeResource CaptionTextBlockStyle}"/>
                        </StackPanel>
                        <TextBox Grid.Row="0" Grid.Column="2"
                                 PlaceholderText="Например: BOSCH"
                                 FontFamily="{StaticResource SFPro}"
                                 MaxLength="80"
                                 Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                        <!-- IsOem -->
                        <FontIcon Grid.Row="1" Grid.Column="0" Glyph="&#xE82D;" VerticalAlignment="Center"/>
                        <StackPanel Grid.Row="1" Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Производитель"
                                       FontFamily="{StaticResource SFPro}"
                                       Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                            <TextBlock Text="Определяет бейдж OEM/Аналог и влияет на фильтры и отчёты."
                                       FontFamily="{StaticResource SFPro}"
                                       Style="{ThemeResource CaptionTextBlockStyle}"/>
                        </StackPanel>
                        <ToggleSwitch Grid.Row="1" Grid.Column="2" OnContent="OEM" OffContent="Аналог"
                                      IsOn="{Binding IsOem, Mode=TwoWay}"/>

                        <!-- Country -->
                        <FontIcon Grid.Row="2" Grid.Column="0" Glyph="&#xE909;" VerticalAlignment="Center"/>
                        <StackPanel Grid.Row="2" Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Страна (ISO-2)"
                                       FontFamily="{StaticResource SFPro}"
                                       Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                            <TextBlock Text="Используется для флага/локализации и фильтров по стране; только ISO-3166-1 alpha-2."
                                       FontFamily="{StaticResource SFPro}"
                                       Style="{ThemeResource CaptionTextBlockStyle}"/>
                        </StackPanel>
                        <ComboBox Grid.Row="2" Grid.Column="2" MinWidth="200"
                                  ItemsSource="{Binding Countries}"
                                  SelectedItem="{Binding SelectedCountry, Mode=TwoWay}"
                                  DisplayMemberPath="Name"/>

                        <!-- Website -->
                        <FontIcon Grid.Row="3" Grid.Column="0" Glyph="&#xE774;" VerticalAlignment="Center"/>
                        <StackPanel Grid.Row="3" Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Сайт"
                                       FontFamily="{StaticResource SFPro}"
                                       Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                            <TextBlock Text="Показываем кнопку «Открыть сайт» и проверяем ссылку; пустое значение отображается как «—»."
                                       FontFamily="{StaticResource SFPro}"
                                       Style="{ThemeResource CaptionTextBlockStyle}"/>
                        </StackPanel>
                        <TextBox Grid.Row="3" Grid.Column="2"
                                 PlaceholderText="https://example.com"
                                 FontFamily="{StaticResource SFPro}"
                                 Text="{Binding Website, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                        <!-- YearFounded -->
                        <FontIcon Grid.Row="4" Grid.Column="0" Glyph="&#xE823;" VerticalAlignment="Center"/>
                        <StackPanel Grid.Row="4" Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Год основания"
                                       FontFamily="{StaticResource SFPro}"
                                       Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                            <TextBlock Text="Используется в профиле бренда и сортировках по возрасту; вне диапазона не принимается."
                                       FontFamily="{StaticResource SFPro}"
                                       Style="{ThemeResource CaptionTextBlockStyle}"/>
                        </StackPanel>
                        <NumberBox Grid.Row="4" Grid.Column="2" Minimum="1850" Maximum="2100"
                                   Value="{Binding YearFounded, Mode=TwoWay}"/>

                        <!-- Description -->
                        <FontIcon Grid.Row="5" Grid.Column="0" Glyph="&#xE8C8;" VerticalAlignment="Center"/>
                        <StackPanel Grid.Row="5" Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Краткое описание"
                                       FontFamily="{StaticResource SFPro}"
                                       Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                            <TextBlock Text="Показывается в карточках и «О бренде», участвует в поиске; обрезается до 400 символов."
                                       FontFamily="{StaticResource SFPro}"
                                       Style="{ThemeResource CaptionTextBlockStyle}"/>
                        </StackPanel>
                        <TextBox Grid.Row="5" Grid.Column="2"
                                 FontFamily="{StaticResource SFPro}"
                                 TextWrapping="Wrap" AcceptsReturn="True" Height="88"
                                 PlaceholderText="Описание бренда…"
                                 Text="{Binding Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>
                </Expander>

                <!-- === АЛИАСЫ === -->
                <Expander x:Name="ExpAliases"
                          Grid.Row="1"
                          Expanding="Accordion_Expanding">
                    <Expander.Header>
                        <TextBlock Text="АЛИАСЫ"
                                   Style="{ThemeResource SubtitleTextBlockStyle}"
                                   FontFamily="{StaticResource SFPro}"
                                   Margin="10,4,10,4"
                                   VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis"/>
                    </Expander.Header>

                    <Grid Margin="10,4,10,4" RowSpacing="12">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- описание блока -->
                        <TextBlock Grid.Row="0"
                                   Text="Используются при поиске и импорте; значения уникальны в пределах бренда, нормализуем регистр и пробелы. Изменения применяются сразу после сохранения."
                                   FontFamily="{StaticResource SFPro}"
                                   Style="{ThemeResource CaptionTextBlockStyle}"/>

                        <StackPanel Grid.Row="1" Orientation="Horizontal">
                            <TextBox PlaceholderText="Новый алиас…" Width="320"
                                     FontFamily="{StaticResource SFPro}"
                                     Text="{Binding NewAliasText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            <Button Content="Добавить" Margin="8,0,0,0" Command="{Binding AddAliasCommand}"/>
                        </StackPanel>

                        <ListView Grid.Row="2" Height="160"
                                  ItemsSource="{Binding Aliases}"
                                  SelectionMode="None">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="models:BrandAlias">
                                    <Grid ColumnSpacing="8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBox Grid.Column="0"
                                                 FontFamily="{StaticResource SFPro}"
                                                 Text="{x:Bind Alias, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                        <Button Grid.Column="1" Content="Удалить"
                                                Command="{Binding DataContext.RemoveAliasCommand, ElementName=PageRoot}"
                                                CommandParameter="{Binding}"/>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                </Expander>

                <!-- === ЦЕНООБРАЗОВАНИЕ === -->
                <Expander x:Name="ExpPricing"
                          Grid.Row="2"
                          Expanding="Accordion_Expanding">
                    <Expander.Header>
                        <TextBlock Text="ЦЕНООБРАЗОВАНИЕ"
                                   Style="{ThemeResource SubtitleTextBlockStyle}"
                                   FontFamily="{StaticResource SFPro}"
                                   Margin="10,4,10,4"
                                   VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis"/>
                    </Expander.Header>

                    <Grid Margin="10,4,10,4" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="420"/>
                        </Grid.ColumnDefinitions>

                        <FontIcon Grid.Column="0" Glyph="&#xEAFD;" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Наценка бренда, %"
                                       FontFamily="{StaticResource SFPro}"
                                       Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                            <TextBlock Text="Используется при расчёте розничной цены по всему бренду; изменения вступят в силу после сохранения."
                                       FontFamily="{StaticResource SFPro}"
                                       Style="{ThemeResource CaptionTextBlockStyle}"/>
                        </StackPanel>
                        <NumberBox Grid.Column="2" Minimum="0" Maximum="200" SmallChange="0.1"
                                   Value="{Binding MarkupPercent, Mode=TwoWay}"/>
                    </Grid>
                </Expander>

                <!-- === ПОЛЬЗОВАТЕЛЬСКИЕ === -->
                <Expander x:Name="ExpUser"
                          Grid.Row="3"
                          Margin="20,0,20,20"
                          Expanding="Accordion_Expanding">
                    <Expander.Header>
                        <TextBlock Text="ПОЛЬЗОВАТЕЛЬСКИЕ"
                                   Style="{ThemeResource SubtitleTextBlockStyle}"
                                   FontFamily="{StaticResource SFPro}"
                                   Margin="10,4,10,4"
                                   VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis"/>
                    </Expander.Header>

                    <Grid Margin="10,4,10,4" RowSpacing="12" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="420"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- UserNotes -->
                        <FontIcon Grid.Row="0" Grid.Column="0" Glyph="&#xE70B;" VerticalAlignment="Center"/>
                        <StackPanel Grid.Row="0" Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Заметки пользователя"
                                       FontFamily="{StaticResource SFPro}"
                                       Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                            <TextBlock Text="Хранятся в ваших настройках и видны только вам."
                                       FontFamily="{StaticResource SFPro}"
                                       Style="{ThemeResource CaptionTextBlockStyle}"/>
                        </StackPanel>
                        <TextBox Grid.Row="0" Grid.Column="2" Height="72"
                                 FontFamily="{StaticResource SFPro}"
                                 AcceptsReturn="True" TextWrapping="Wrap"
                                 PlaceholderText="Короткие заметки…"
                                 Text="{Binding UserNotes, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>
                </Expander>
            </Grid>
        </ScrollViewer>

        <!-- Фиксированная кнопка сохранения -->
        <Button Content="Сохранить изменения"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Margin="20,10,20,10"
                Command="{Binding SaveCommand}"/>
    </Grid>
</Page>
