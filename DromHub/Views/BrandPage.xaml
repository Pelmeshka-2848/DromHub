<Page
    x:Class="DromHub.Views.BrandPage"
    x:Name="PageRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:converters="using:DromHub.Converters"
    mc:Ignorable="d">

    <Page.Resources>
        <!-- Конвертер для кнопки добавления синонима -->
        <converters:NullToBooleanConverter x:Key="NullToBooleanConverter"/>

        <!-- Стиль для ListView -->
        <Style TargetType="ListView">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>

        <!-- Стиль для элементов ListView -->
        <Style TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>

        <CollectionViewSource x:Key="BrandCvs" Source="{Binding GroupedBrands}" IsSourceGrouped="True" ItemsPath="Items"/>
    </Page.Resources>

    <Grid Padding="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Поисковая строка -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8">
            <TextBox 
                PlaceholderText="Поиск брендов..."
                Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                Width="300"
                KeyDown="SearchBox_KeyDown"/>

            <Button 
                Content="Найти"
                Command="{Binding SearchBrandsCommand}"
                MinWidth="100"/>
        </StackPanel>

        <!-- Основной контент -->
        <Grid Grid.Row="1" Margin="0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Список брендов с группировкой -->
            <ListView Grid.Column="0"
                      ItemsSource="{Binding Source={StaticResource BrandCvs}}"
                      SelectionMode="Single"
                      SelectedItem="{Binding SelectedBrand, Mode=TwoWay}"
                      VerticalAlignment="Stretch">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid ColumnDefinitions="*, Auto" Padding="8" VerticalAlignment="Center">
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="{Binding Name}"
                                           FontSize="14" 
                                           VerticalAlignment="Center"
                                           Foreground="Black"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>

                <ListView.GroupStyle>
                    <GroupStyle>
                        <GroupStyle.HeaderTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Key}"
                                           FontWeight="Bold" 
                                           FontSize="16"
                                           Margin="8,8,8,4"
                                           Foreground="Black"/>
                            </DataTemplate>
                        </GroupStyle.HeaderTemplate>
                    </GroupStyle>
                </ListView.GroupStyle>
            </ListView>

            <!-- Вертикальный разделитель -->
            <Rectangle Grid.Column="1" Fill="{ThemeResource DividerStrokeColorDefaultBrush}" Width="1"/>

            <!-- Правая панель с синонимами -->
            <StackPanel Grid.Column="2">
                <TextBlock Text="Синонимы:" FontWeight="Bold" Margin="0,0,0,8" Foreground="Black"/>

                <!-- Список синонимов -->
                <ListView 
                    ItemsSource="{Binding Aliases}" 
                    SelectedItem="{Binding SelectedAlias, Mode=TwoWay}"
                    Height="200"
                    x:Name="AliasesList">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Alias}" Width="200" Foreground="Black"/>
                                <TextBlock Text="{Binding Note}" Width="150" Foreground="Black"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <!-- Кнопки управления синонимами -->
                <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,8,0,0">
                    <Button 
    Content="Редактировать"
    Command="{Binding EditAliasCommand}"
    CommandParameter="{Binding ElementName=PageRoot, Path=XamlRoot}"
    IsEnabled="{Binding SelectedAlias, Converter={StaticResource NullToBooleanConverter}}"
    MinWidth="100"/>

                    <Button 
    Content="Удалить"
    Command="{Binding DeleteAliasCommand}"
    CommandParameter="{Binding ElementName=PageRoot, Path=XamlRoot}"
    IsEnabled="{Binding SelectedAlias, Converter={StaticResource NullToBooleanConverter}}"
    MinWidth="100"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- Кнопки действий -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Spacing="12"
                    Margin="0,12,0,0">

            <Button Content="Добавить бренд"
                    Click="AddBrand_Click"
                    MinWidth="120"
                    Padding="12,6"/>

            <Button Content="Добавить синоним"
                    Command="{Binding AddAliasCommand}"
                    MinWidth="120"
                    Padding="12,6"
                    IsEnabled="{Binding SelectedBrand, Converter={StaticResource NullToBooleanConverter}}"/>
        </StackPanel>
    </Grid>
</Page>