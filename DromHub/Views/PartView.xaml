<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="DromHub.Views.PartView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DromHub.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Бренд -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8">
            <TextBlock Text="Бренд:" VerticalAlignment="Center" Width="120"/>
            <ComboBox 
                ItemsSource="{x:Bind ViewModel.Brands, Mode=OneWay}"
                SelectedItem="{x:Bind ViewModel.SelectedBrand, Mode=TwoWay}"
                DisplayMemberPath="Name"
                Width="300"/>
        </StackPanel>

        <!-- Каталожный номер -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8" Margin="0,8,0,0">
            <TextBlock Text="Каталожный номер:" VerticalAlignment="Center" Width="120"/>
            <TextBox 
                Text="{x:Bind ViewModel.CatalogNumber, Mode=TwoWay}"
                Width="300"/>
        </StackPanel>

        <!-- Название -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8" Margin="0,8,0,0">
            <TextBlock Text="Название:" VerticalAlignment="Center" Width="120"/>
            <TextBox 
                Text="{x:Bind ViewModel.Name, Mode=TwoWay}"
                Width="300"/>
        </StackPanel>

        <!-- Артикул (только чтение) -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="8" Margin="0,8,0,0">
            <TextBlock Text="Артикул:" VerticalAlignment="Center" Width="120"/>
            <TextBlock 
                Text="{x:Bind ViewModel.Article, Mode=OneWay}"
                Width="300"/>
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" Spacing="8" Margin="0,16,0,0">
            <Button 
                Content="Сохранить"
                Command="{x:Bind ViewModel.SavePartCommand}"
                Width="120"/>
        </StackPanel>
    </Grid>
</Page>
