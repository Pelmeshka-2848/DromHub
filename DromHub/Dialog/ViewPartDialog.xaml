<ContentDialog
    x:Class="DromHub.Views.ViewPartDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Просмотр детали"
    PrimaryButtonText="Закрыть"
    DefaultButton="Primary"
    Width="1200"
    Height="900">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Верхняя часть - изображение -->
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Spacing="15" Padding="16" HorizontalAlignment="Center">
                <!-- Область изображения -->
                <Border Background="{ThemeResource CardBackgroundFillColorDefault}" 
                        Height="400" 
                        CornerRadius="8"
                        BorderBrush="{ThemeResource CardStrokeColorDefault}"
                        BorderThickness="1"
                        Width="400">

                    <Grid>
                        <!-- Картинка -->
                        <Image x:Name="PartImage"
                               Stretch="Uniform"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               MaxWidth="380"
                               MaxHeight="380"
                               ImageFailed="PartImage_ImageFailed"/>

                        <!-- Заглушка "нет изображения" -->
                        <StackPanel x:Name="NoImagePlaceholder"
                                    HorizontalAlignment="Center" 
                                    VerticalAlignment="Center" 
                                    Spacing="10"
                                    Visibility="Collapsed">
                            <TextBlock Text="📷" FontSize="64" HorizontalAlignment="Center"/>
                            <TextBlock Text="Нет изображения" 
                                       FontSize="18"
                                       HorizontalAlignment="Center"
                                       Foreground="{ThemeResource TextFillColorSecondary}"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Нижняя часть - информация и кроссы -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="25" Padding="20">
                <!-- Основная информация -->
                <StackPanel Spacing="15">
                    <TextBlock Text="Основная информация" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,15"/>

                    <Grid HorizontalAlignment="Left">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="180"/>
                            <ColumnDefinition Width="400"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="Название:" FontWeight="SemiBold" FontSize="15" Grid.Row="0" Grid.Column="0"/>
                        <TextBlock Text="{Binding Part.Name}" TextWrapping="Wrap" FontSize="15" Grid.Row="0" Grid.Column="1" Margin="12,0,0,0"/>

                        <TextBlock Text="Каталожный №:" FontWeight="SemiBold" FontSize="15" Grid.Row="1" Grid.Column="0" Margin="0,12,0,0"/>
                        <TextBlock Text="{Binding Part.CatalogNumber}" FontSize="15" Grid.Row="1" Grid.Column="1" Margin="12,12,0,0"/>

                        <TextBlock Text="Артикул:" FontWeight="SemiBold" FontSize="15" Grid.Row="2" Grid.Column="0" Margin="0,12,0,0"/>
                        <TextBlock Text="{Binding Part.Article}" FontSize="15" Grid.Row="2" Grid.Column="1" Margin="12,12,0,0"/>

                        <TextBlock Text="Бренд:" FontWeight="SemiBold" FontSize="15" Grid.Row="3" Grid.Column="0" Margin="0,12,0,0"/>
                        <TextBlock Text="{Binding Part.Brand.Name}" FontSize="15" Grid.Row="3" Grid.Column="1" Margin="12,12,0,0"/>
                    </Grid>
                </StackPanel>

                <!-- Список кроссов -->
                <StackPanel>
                    <TextBlock Text="Кроссы (альтернативные номера)" 
                               FontSize="20" FontWeight="SemiBold" Margin="0,0,0,15"/>

                    <Border BorderBrush="{ThemeResource CardStrokeColorDefault}" 
                            BorderThickness="1" 
                            CornerRadius="4"
                            Height="400">
                        <ListView x:Name="CrossesListView" 
                                  ItemsSource="{Binding Crosses}" 
                                  ScrollViewer.VerticalScrollBarVisibility="Auto">
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    <Setter Property="Padding" Value="0"/>
                                    <Setter Property="MinHeight" Value="45"/>
                                </Style>
                            </ListView.ItemContainerStyle>

                            <ListView.Header>
                                <Grid Padding="16" Background="{ThemeResource SystemControlBackgroundAltHighBrush}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="1.5*"/>
                                        <ColumnDefinition Width="3*"/>
                                        <ColumnDefinition Width="1.5*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Text="Каталожный №" FontWeight="SemiBold" FontSize="15" Grid.Column="0"/>
                                    <TextBlock Text="Бренд" FontWeight="SemiBold" FontSize="15" Grid.Column="1"/>
                                    <TextBlock Text="Примечание" FontWeight="SemiBold" FontSize="15" Grid.Column="2"/>
                                    <TextBlock Text="Тип" FontWeight="SemiBold" FontSize="15" Grid.Column="3"/>
                                </Grid>
                            </ListView.Header>

                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid Padding="16" BorderBrush="{ThemeResource CardStrokeColorDefault}" BorderThickness="0,0,0,1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="2*"/>
                                            <ColumnDefinition Width="1.5*"/>
                                            <ColumnDefinition Width="3*"/>
                                            <ColumnDefinition Width="1.5*"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Text="{Binding CatalogNumber}" FontSize="15" Grid.Column="0" TextWrapping="Wrap" Margin="0,0,8,0"/>
                                        <TextBlock Text="{Binding Brand}" FontSize="15" Grid.Column="1" TextWrapping="Wrap" Margin="0,0,8,0"/>
                                        <TextBlock Text="{Binding Note}" FontSize="15" Grid.Column="2" TextWrapping="Wrap" Margin="0,0,8,0"/>
                                        <TextBlock Text="{Binding RelationType}" FontSize="15" Grid.Column="3" TextWrapping="Wrap"/>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Border>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</ContentDialog>